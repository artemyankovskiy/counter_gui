1. apache configuration:

# Убедитесь что эта строка отображается лишь однажды
NameVirtualHost 127.0.0.1:8080

# А это конфигурация Вашего проекта
Listen 127.0.0.1:8080

<VirtualHost 127.0.0.1:8080>
  DocumentRoot "/home/counter_gui/web"
  DirectoryIndex index.php
  <Directory "/home/counter_gui/web">
    AllowOverride All
    Allow from All
  </Directory>

  Alias /sf /home/counter_gui/lib/vendor/symfony/data/web/sf
  <Directory "/home/counter_gui/lib/vendor/symfony/data/web/sf">
    AllowOverride All
    Allow from All
  </Directory>
</VirtualHost>

2. create database 
echo "CREATE SCHEMA db DEFAULT CHARACTER SET utf8 COLLATE utf8_general_ci" | mysql -u root -p

3. configure counter_gui
$ cd /home/counter_gui
$ ./symfony configure:database "mysql:unix_socket=/var/run/mysql/mysql.sock;dbname=db" root Password
$ ./symfony doctrine:build --all --and-load


4. web test
http://127.0.0.1:8080/countersValues
